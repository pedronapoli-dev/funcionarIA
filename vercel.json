{
  "buildCommand": "if [ -f package.json ] && grep -q 'workspaces' package.json; then npx turbo run build --filter=@educarseia/web --dangerously-disable-package-manager-check; else cd ../.. && npx turbo run build --filter=@educarseia/web --dangerously-disable-package-manager-check; fi",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
